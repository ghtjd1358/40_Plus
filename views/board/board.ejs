<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>board</title>

    <!-- 게시물 저장하고 DB로 전송하는 스크립트 -->

    <style>
      .boardContainer {
        display: flex;
        flex-direction: column; /* 열 방향으로 추가 */
        width: 100%;
        height: 100%;
      }
      .postBox {
        flex-direction: column;
        width: 50%;
        height: 100px;
        border: 3px solid;
        margin-bottom: 10px; /* 추가된 부분 */
      }
    </style>

    <!-- jQuery cdn -->
    <script
      src="https://code.jquery.com/jquery-3.7.1.min.js"
      integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo="
      crossorigin="anonymous"
    ></script>

    <!-- axios cdn -->
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  </head>
  <body>
    <h1>board</h1>
    <button type="button" id="CreateBoardBtn">게시물 생성하기</button>
    <a href="/boardList">게시물 확인하기</a>

    <br />
    <hr />
    <div class="boardContainer">
      <!-- 초기에 하나의 postBox가 있을 때, 복제할 대상이 되어야 함 -->
      <div class="postBox" id="originalPostBox">
        <form name="postForm">
          <input type="text" id="title" placeholder="글 제목" />
          <input type="text" id="content" placeholder="content" />
          <button type="button" id="contentBtn" onclick="getForm()">
            제출
          </button>
        </form>
      </div>
    </div>

    <script>
      const btn = document.querySelector("#CreateBoardBtn");
      const boardContainer = document.querySelector(".boardContainer");
      const originalPostBox = document.querySelector("#originalPostBox");

      btn.addEventListener("click", addPostBox);

      function addPostBox() {
        // originalPostBox를 복제하여 새로운 postBox 생성
        const newPostBox = originalPostBox.cloneNode(true);
        boardContainer.appendChild(newPostBox);
      }

      function getForm() {
        const form = document.forms["postForm"];
        console.log("form", form);

        axios({
          method: "post",
          url: "/board",
          data: {
            title: form.title.value,
            content: form.content.value,
          },
        }).then((res) => {
          console.log("axios 전송");
        });
      }
    </script>
  </body>
</html>
