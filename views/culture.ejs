<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <link rel="stylesheet" href="/static/css/header.css" />
    <link rel="stylesheet" href="/static/css/component.css" />
    <title>culture</title>

    <style>
      .mainContent {
        display: flex;
        width: 80%;
        /* background-color: pink; */
        align-items: center;
        justify-content: center;
      }

      .content-box {
        width: 300px;
        height: 400px;

        margin: 0 var(--space-1);
        background-color: white;
        border: 1px solid var(--color-black-1);
        border-radius: var(--border-radius-medium);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        position: relative;
        overflow: hidden;
      }

      .content-box img {
        width: 100%;
        height: 100%;
        display: block;
        object-fit: cover;
        object-position: center top;
        /* object-position: 50% 50%; */
        /* opacity: 0.5; */
        filter: grayscale(60%);
      }

      .content img {
        transition: all 0.4s linear;
      }

      .content:hover img {
        transform: scale(1.4);
        filter: grayscale(100%);
      }

      .content a {
        transition: all 0.5s linear;
      }

      .content:hover a {
        color: var(--color-white-0);
        font-size: 30px;
      }

      /* .class {
        background-image: url("/static//css/university-105709_1280.jpg");
      } */

      .textButton {
        position: absolute;
        width: 100%;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        color: var(--color-black-0);
        font-size: 0;
        font-weight: bold;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <%- include("./component/header")%>

    <main class="container">
      <div class="space"></div>
      <section class="mainContent">
        <div class="content-box content class">
          <img src="/static//css/university-105709_1280.jpg" alt="" />
          <a href="/culture/class" class="textButton"> 지역별 일일 클래스 </a>
        </div>
        <div class="content-box content">
          <img src="/static//css/books-1281581_1280.jpg" alt="" />
          <a href="/culture/library" class="textButton"> 지역별 도서관 정보</a>
        </div>
        <div class="content-box content">
          <a href="/" class="menu"> <img src="" alt="" />???</a>
        </div>
      </section>
    </main>
  </body>
</html>
